% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_Ainv.R
\name{build_Ainv}
\alias{build_Ainv}
\title{Build the sparse inverse of the numerator relationship matrix (A-inverse)}
\usage{
build_Ainv(renum)
}
\arguments{
\item{renum}{A data frame as returned by \code{\link[=renum_pedigree]{renum_pedigree()}}, with columns
\code{new_id}, \code{new_sire}, and \code{new_dam} (integer, 0 = unknown).}
}
\value{
A list with:
\describe{
\item{Ainv}{A sparse matrix of class \code{dgCMatrix} (N x N), the inverse of
the numerator relationship matrix.}
\item{F}{Numeric vector of length N with individual inbreeding coefficients.}
}
}
\description{
Constructs \eqn{A^{-1}} directly from a renumbered pedigree using the
Henderson rules, without ever forming \eqn{A} explicitly. Individual
inbreeding coefficients are computed internally via an ML92-style traversal.
}
\details{
The pedigree must be in topological order (parents before offspring), as
produced by \code{\link[=renum_pedigree]{renum_pedigree()}}. The heavy computations are delegated to
compiled C++ code for efficiency with large pedigrees.
}
\examples{
ped <- data.frame(
  animal = c("A", "B", "C", "D"),
  sire   = c("0", "0", "A", "A"),
  dam    = c("0", "0", "B", "B")
)
renum <- renum_pedigree(ped, verbose = FALSE)
Ainv_res <- build_Ainv(renum)
Ainv_res$Ainv

}
\seealso{
\code{\link[=renum_pedigree]{renum_pedigree()}}, \code{\link[=build_Ginv]{build_Ginv()}}, \code{\link[=build_Hinv]{build_Hinv()}},
\code{\link[=compute_connectedness]{compute_connectedness()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_Ginv.R
\name{build_Ginv}
\alias{build_Ginv}
\title{Build the dense inverse of the genomic relationship matrix (G-inverse)}
\usage{
build_Ginv(
  X,
  maf_threshold = 0.05,
  missing_code = 5L,
  blend = 0.05,
  chunk_size = 2000L,
  n_threads = 1L,
  tunedG = 0L,
  A22 = NULL,
  verbose = TRUE
)
}
\arguments{
\item{X}{Genotype matrix of dimension \verb{n_gen x m}, coded as 0/1/2.
Missing genotypes must be coded with \code{missing_code}.}

\item{maf_threshold}{Minor allele frequency threshold used to filter SNPs.}

\item{missing_code}{Integer code representing missing genotypes.}

\item{blend}{Blending factor applied to \eqn{G} before optional tuning.}

\item{chunk_size}{Number of SNP columns processed per chunk.}

\item{n_threads}{Number of OpenMP threads used by the compiled backend
during genomic matrix construction and related parallel steps.}

\item{tunedG}{Integer tuning option for \eqn{G}: \code{0} = no tuning; \code{1} = standardize by matching mean diagonal/off-diagonal contrast within \eqn{G}; \code{2} = affine tuning to match \eqn{A_{22}} mean diagonal and off-diagonal; \code{3} = shift \eqn{G} by a constant so its global mean matches \eqn{A_{22}}.}

\item{A22}{Optional dense pedigree-based relationship matrix for the same
genotyped animals and in the same order as the rows of \code{X}. Required when
\code{tunedG} is 2 or 3.}
}
\value{
A list containing \code{Ginv} and several diagnostics describing SNP
filtering and tuning.
}
\description{
Computes a dense \eqn{G^{-1}} for the genotyped animals using a VanRaden
method-1 style construction of \eqn{G}, followed by matrix inversion.
Optional affine tuning of \eqn{G} can be applied before inversion.
}
\details{
The matrix \code{X} is coerced to an integer matrix before being passed to the
compiled backend. When \code{tunedG} equals 2 or 3, \code{A22} must be supplied and
must have dimensions \verb{n_gen x n_gen}.
}
\seealso{
\code{\link[=build_Ainv]{build_Ainv()}}, \code{\link[=build_Hinv]{build_Hinv()}}
}

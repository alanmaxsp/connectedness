% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_Hinv.R
\name{build_Hinv}
\alias{build_Hinv}
\title{Build the sparse inverse of the H matrix}
\usage{
build_Hinv(
  renum,
  genotyped_idx,
  X,
  maf_threshold = 0.05,
  missing_code = 5L,
  blend = 0.05,
  chunk_size = 2000L,
  n_threads = 1L,
  tunedG = 0L,
  tau = 1,
  omega = 1,
  return_Ainv = FALSE,
  return_F = FALSE,
  return_A22 = FALSE,
  return_Ginv = FALSE,
  return_allele_freqs = FALSE
)
}
\arguments{
\item{renum}{A data frame as returned by \code{\link[=renum_pedigree]{renum_pedigree()}}, with columns
\code{new_id}, \code{new_sire}, and \code{new_dam}.}

\item{genotyped_idx}{Integer vector with the 1-based renumbered pedigree
indices of the genotyped animals, in the same order as the rows of \code{X}.}

\item{X}{Genotype matrix of dimension \verb{n_gen x m}, coded as 0/1/2.
Missing genotypes must be coded with \code{missing_code}.}

\item{maf_threshold}{Minor allele frequency threshold used to filter SNPs.}

\item{missing_code}{Integer code representing missing genotypes.}

\item{blend}{Blending factor applied to \eqn{G} before optional tuning.}

\item{chunk_size}{Number of SNP columns processed per chunk.}

\item{n_threads}{Number of OpenMP threads.}

\item{tunedG}{Integer tuning option for \eqn{G}. Use 0 for no tuning.}

\item{tau}{Scaling factor multiplying \eqn{G^{-1}} in the final expression.}

\item{omega}{Scaling factor multiplying \eqn{A_{22}^{-1}} in the final expression.}

\item{return_Ainv}{Logical; return \code{Ainv} in the output list.}

\item{return_F}{Logical; return inbreeding coefficients \code{F} in the output list.}

\item{return_A22}{Logical; return \code{A22} in the output list.}

\item{return_Ginv}{Logical; return \code{Ginv} in the output list.}

\item{return_allele_freqs}{Logical; return retained SNP allele frequencies.}
}
\value{
A list containing \code{Hinv} and, optionally, intermediate matrices and
diagnostics.
}
\description{
Computes \eqn{H^{-1}} from a renumbered pedigree and a genotype matrix using
a combined pedigree-genomic relationship structure (H-kernel). Internally,
the function performs the full pipeline:
\enumerate{
\item build \eqn{A^{-1}},
\item compute individual inbreeding coefficients,
\item construct \eqn{A_{22}},
\item build and optionally tune \eqn{G}, then invert it,
\item combine all components to obtain \eqn{H^{-1}}.
}
}
\seealso{
\code{\link[=renum_pedigree]{renum_pedigree()}}, \code{\link[=build_Ainv]{build_Ainv()}}, \code{\link[=build_Ginv]{build_Ginv()}}
}
